import React from 'react';
import ReactDOM from 'react-dom/client';
import NikunjApp from './App';
import './Dashboard.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <NikunjApp />
  </React.StrictMode>
);
